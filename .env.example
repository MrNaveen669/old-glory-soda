# Environment Variables Example for Render Deployment

# Server Port (automatically injected by Render)
PORT=10000

# Server Host binding (0.0.0.0 for Render / public container binding)
HOST=0.0.0.0

# Node Environment
NODE_ENV=production
